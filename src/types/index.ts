export type Recipe = {
    title: string,
    ingredients: string,
    instructions: string,
    source: string,
    imageUrl: string,
    language?: string,
    createdAt: string,
    updatedAt: string,
    translatedTitle?: string,
    image?: string,
}